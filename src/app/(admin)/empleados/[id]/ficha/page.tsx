/**
 * Ficha detallada de un Employee (`/empleados/[id]/ficha`).
 * Datos maestros del colaborador para RRHH y Payroll.
 */

import { ModulePlaceholder } from "@/components/layout/module-placeholder";

/** Vista de ficha de Employee por `id`. */
export default function FichaEmpleadoPage() {
  return <ModulePlaceholder title="Ficha de empleado" />;
}

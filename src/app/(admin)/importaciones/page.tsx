/**
 * Importaciones masivas de datos al ERP.
 * Cargas CSV/Excel hacia maestros (Product, Customer, Employee, etc.).
 */

import { ModulePlaceholder } from "@/components/layout/module-placeholder";

/** Pantalla de importaciones. */
export default function ImportacionesPage() {
  return <ModulePlaceholder title="Importaciones" />;
}
